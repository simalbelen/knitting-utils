import type { Section } from "../../types/Section";

interface Props {
  onClose: () => void;
  section: Section;
}

function EditSectionForm({ onClose, section }: Props) {
  return <></>;
}

export default EditSectionForm;
